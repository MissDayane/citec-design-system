// ===================================
// SHAPE — CITEC DESIGN SYSTEM
// Bordas e arredondamentos
// ===================================

// Radius padrão global do sistema
const shape = {
  borderRadius: 12, // 12px
};

// Tokens adicionais (opcional — usados em componentes customizados)
export const radius = {
  xxs: 4,    // 4px
  xs: 6,     // 6px
  sm: 8,     // 8px
  md: 12,    // 12px (padrão)
  lg: 16,    // 16px
  xl: 20,    // 20px
  pill: 999, // totalmente arredondado
};

export default shape;
