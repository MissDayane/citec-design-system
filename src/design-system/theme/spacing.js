// ===================================
// SPACING — CITEC DESIGN SYSTEM
// Escala baseada em 4px para maior flexibilidade
// ===================================

// Função usada pelo MUI: spacing(2) = 8px
const spacing = 4;

export const spacingScale = {
  xxs: spacing * 0.5,   // 2px
  xs: spacing * 1,      // 4px
  sm: spacing * 2,      // 8px
  md: spacing * 3,      // 12px
  lg: spacing * 4,      // 16px
  xl: spacing * 6,      // 24px
  xxl: spacing * 8,     // 32px
};

export default spacing;
